{
  "name": "released",
  "description": "Keep up with new music from your favorite labels 📡",
  "dependencies": {
    "@emotion/core": "^10.0.22",
    "gatsby": "^2.18.7",
    "gatsby-plugin-emotion": "^4.1.16",
    "gatsby-plugin-netlify": "^2.1.27",
    "gatsby-plugin-react-helmet": "^3.1.16",
    "gatsby-plugin-remove-trailing-slashes": "^2.1.15",
    "react": "^16.11.0",
    "react-dom": "^16.11.0",
    "react-helmet": "^5.2.1"
  },
  "devDependencies": {
    "prettier": "^1.19.1"
  },
  "scripts": {
    "build": "gatsby build",
    "format": "prettier --write '**/*.{js,json}'",
    "start": "gatsby develop",
    "test:labels": "cd functions && node -e \"require('.').searchLabels({ query: { search: process.argv[1] } }, { set: () => {}, status: () => ({ send: r => console.log(r) }) })\"",
    "test:latest": "cd functions && node -e \"require('.').searchLabels({ query: { label: process.argv[1] } }, { set: () => {}, status: () => ({ send: r => console.log(r) }) })\"",
    "deploy:functions": "cd functions && gcloud functions deploy searchLabels --region=europe-west1 --runtime nodejs10 --trigger-http"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/luukdv/released.git"
  }
}
