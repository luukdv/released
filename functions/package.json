{
  "scripts": {
    "test": "node -e \"require('.').searchLabels({ query: { search: process.argv[1] } }, { status: () => ({ send: r => console.log(r) }) })\"",
    "deploy": "gcloud functions deploy searchLabels --region=europe-west1 --runtime nodejs10 --trigger-http"
  },
  "dependencies": {
    "got": "^9.6.0"
  }
}
